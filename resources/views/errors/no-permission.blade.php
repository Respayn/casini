@component('layouts.app', ['title' => __('permissions.denied')])
    <div class="flex h-full min-h-64 items-center justify-center">
        <p class="text-secondary-text text-lg">{{ __('permissions.denied') }}</p>
    </div>
@endcomponent
